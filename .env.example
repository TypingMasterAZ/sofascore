# Email Configuration (Nodemailer)
EMAIL_USER=typingmaster.az@gmail.com
EMAIL_PASS=hlwg iaey ryxn klsq

# Firebase Configuration
# Paste the entire content of serviceAccountKey.json here (as a single string if possible, or handle in code)
# In Render, paste the whole JSON object.
FIREBASE_SERVICE_ACCOUNT='{"type": "service_account", ...}'

# Server Port
PORT=3000
